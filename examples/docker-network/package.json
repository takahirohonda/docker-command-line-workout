{
  "name": "docker-network",
  "version": "1.0.0",
  "description": "```bash # create a network docker network create --driver=bridge app-net",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^21.3.10",
    "mongodb": "^6.8.0"
  }
}
